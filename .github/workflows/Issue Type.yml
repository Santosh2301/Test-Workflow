name: Issue Trigger

on:
  issues:
    types: [opened]   # Runs ONLY when a new issue is opened

jobs:
  run_on_issue:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "An issue was opened — workflow triggered successfully!"
